package com.cometchat.pro.uikit.ui_settings.enum

enum class GroupMode(private val label: String) {
    PUBLIC_GROUP("public_groups"), PASSWORD_GROUP("password_protected_groups"), ALL_GROUP("public_and_password_protected_groups");
}

