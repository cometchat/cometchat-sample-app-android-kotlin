package com.cometchat.uikit.kotlin.shared.interfaces

/**
 * Simple click callback interface.
 * Used for handling click events on UI components.
 */
fun interface OnClick {
    /**
     * Called when the view is clicked.
     */
    fun onClick()
}
