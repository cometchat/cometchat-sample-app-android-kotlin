<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <queries>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
            <data android:mimeType="image/* video/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK" />
            <data android:mimeType="image/* video/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.CHOOSER" />
        </intent>
    </queries>

    <application
        tools:node="merge"
        tools:targetApi="m">

        <meta-data
            android:name="com.cometchat.chatuikit.jetpack"
            android:value="${applicationId}" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_path" />
        </provider>
        
        <!-- Call activity for outgoing and ongoing calls -->
        <activity
            android:name=".calls.CometChatCallActivity"
            android:exported="false"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:launchMode="singleTop" />

        <!-- Ongoing call activity with PIP support -->
        <!-- Validates: Requirement 9.6 -->
        <activity
            android:name=".presentation.ongoingcall.ui.CometChatOngoingCallActivity"
            android:exported="false"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout"
            android:excludeFromRecents="true" />

        <!-- Image viewer activity with transparent background for drag-to-dismiss -->
        <!-- Validates: Requirement 1.1 -->
        <activity
            android:name=".presentation.imageviewer.ui.CometChatImageViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.JetpackUIKit.Transparent" />

    </application>

</manifest>
